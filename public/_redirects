# ── Obra Nova SPA — Netlify Redirects ────────────────────────────────────────
# Tutte le routes vengono gestite da React (SPA)
# La landing page è su / — l'app è su /app
# Le firme pubbliche su /firma/:token

/*    /index.html    200
